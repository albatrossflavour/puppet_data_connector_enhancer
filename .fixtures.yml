---
# This file can be used to install module dependencies for unit testing
# See https://github.com/puppetlabs/puppetlabs_spec_helper#using-fixtures for details
fixtures:
  forge_modules:
    stdlib: "puppetlabs/stdlib"
    systemd: "puppet/systemd"
  symlinks:
    # Premium PE module stub: provides the class declaration needed by
    # pre_condition without requiring the licensed module in CI.
    puppet_data_connector: "../../stubs/puppet_data_connector"
