---
phase: 01-orchestrator-and-classification-metrics
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - spec/classes/puppet_data_connector_enhancer_spec.rb
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "rspec-puppet tests pass for orchestrator parameter compilation"
    - "rspec-puppet tests pass for classification parameter compilation"
    - "Boundary validation tests reject out-of-range orchestrator_jobs_limit values"
    - "Template content tests verify collector method names appear in generated script"
  artifacts:
    - path: "spec/classes/puppet_data_connector_enhancer_spec.rb"
      provides: "Test contexts for orchestrator and classification metrics"
      contains: "context 'with orchestrator metrics enabled'"
  key_links:
    - from: "spec/classes/puppet_data_connector_enhancer_spec.rb"
      to: "manifests/init.pp"
      via: "rspec-puppet compiles the class with test parameters"
      pattern: "is_expected.to compile.with_all_deps"
    - from: "spec/classes/puppet_data_connector_enhancer_spec.rb"
      to: "templates/puppet_data_connector_enhancer.epp"
      via: "with_content matchers verify template output"
      pattern: "with_content.*collect_orchestrator"
---

<objective>
Apply the rspec-puppet test contexts from the feature branch and validate that all tests
pass against the merged code from Plan 01. This confirms the orchestrator and classification
integration is working correctly at the manifest compilation level.

Purpose: Prove the merge is correct by running the existing test suite with the new
orchestrator and classification test contexts (per D-07).

Output: Passing rspec-puppet test suite covering orchestrator/classification parameters,
template content validation, and boundary checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-orchestrator-and-classification-metrics/01-CONTEXT.md
@.planning/phases/01-orchestrator-and-classification-metrics/01-01-SUMMARY.md
@spec/classes/puppet_data_connector_enhancer_spec.rb

<interfaces>
<!-- Test contexts to be added from the feature branch patch. -->
<!-- These test six scenarios across two feature areas. -->

Test contexts (from feature branch):
1. 'with orchestrator metrics enabled' - compile test, content checks for collect_orchestrator_jobs,
   collect_orchestrator_plans, enable_orchestrator_metrics: true, orchestrator_jobs_limit: 100,
   fetch_json_from_orchestrator
2. 'with orchestrator metrics disabled (default)' - verifies enable_orchestrator_metrics: false
3. 'with custom orchestrator_jobs_limit' - compile test, content check for limit value 25
4. 'orchestrator parameter validation' - two sub-contexts:
   - orchestrator_jobs_limit=201 raises compile error
   - orchestrator_jobs_limit=0 raises compile error
5. 'with classification metrics enabled' - compile test, content checks for collect_node_groups,
   collect_class_usage, enable_classification_metrics: true
6. 'with classification metrics disabled (default)' - verifies enable_classification_metrics: false

All contexts are inserted before the existing 'resource ordering' context block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply test contexts from feature branch patch</name>
  <files>
    spec/classes/puppet_data_connector_enhancer_spec.rb
  </files>
  <read_first>
    spec/classes/puppet_data_connector_enhancer_spec.rb
    /tmp/patch-puppet_data_connector_enhancer_spec.rb.patch
  </read_first>
  <action>
Per D-07: Keep the existing rspec-puppet manifest tests from the feature branch.
Per D-08: Do NOT add Ruby unit tests for collection logic (deferred).

Try `git apply --3way /tmp/patch-puppet_data_connector_enhancer_spec.rb.patch`.
The research indicates this should apply with offset adjustment only, since the
custom_queries tests on self_service_graphs are in a separate context block.

If the patch does not apply cleanly, manually insert the 108 lines of test contexts.
Find the line containing `context 'resource ordering'` in the spec file and insert
ALL SIX test contexts immediately before it:

1. `context 'with orchestrator metrics enabled'` -- params: enable_orchestrator_metrics=true,
   orchestrator_jobs_limit=100. Tests: compile, content matches for collect_orchestrator_jobs,
   collect_orchestrator_plans, enable_orchestrator_metrics:.*true, orchestrator_jobs_limit:.*100,
   def fetch_json_from_orchestrator.

2. `context 'with orchestrator metrics disabled (default)'` -- no extra params.
   Tests: content matches enable_orchestrator_metrics:.*false.

3. `context 'with custom orchestrator_jobs_limit'` -- params: orchestrator_jobs_limit=25.
   Tests: compile, content matches orchestrator_jobs_limit:.*25.

4. `context 'orchestrator parameter validation'` -- two sub-contexts:
   - params: orchestrator_jobs_limit=201 -> compile.and_raise_error
   - params: orchestrator_jobs_limit=0 -> compile.and_raise_error

5. `context 'with classification metrics enabled'` -- params: enable_classification_metrics=true.
   Tests: compile, content matches for collect_node_groups, collect_class_usage,
   enable_classification_metrics:.*true.

6. `context 'with classification metrics disabled (default)'` -- no extra params.
   Tests: content matches enable_classification_metrics:.*false.

All test contexts use `super().merge(...)` for params to inherit the base parameter set.
All content checks use the deployed script path:
`'/opt/puppetlabs/puppet_data_connector_enhancer/puppet_data_connector_enhancer'`.
All regex patterns use `%r{...}` syntax per project conventions.

Ensure proper RSpec nesting (all contexts sit inside the existing top-level describe/context
blocks at the correct depth, not accidentally nested inside custom_queries contexts).
  </action>
  <verify>
    <automated>grep -q "context 'with orchestrator metrics enabled'" spec/classes/puppet_data_connector_enhancer_spec.rb && grep -q "context 'orchestrator parameter validation'" spec/classes/puppet_data_connector_enhancer_spec.rb && grep -q "context 'with classification metrics enabled'" spec/classes/puppet_data_connector_enhancer_spec.rb && grep -q "context 'with classification metrics disabled (default)'" spec/classes/puppet_data_connector_enhancer_spec.rb && echo "TEST_CONTEXTS_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - spec file contains `context 'with orchestrator metrics enabled'`
    - spec file contains `context 'with orchestrator metrics disabled (default)'`
    - spec file contains `context 'with custom orchestrator_jobs_limit'`
    - spec file contains `context 'orchestrator parameter validation'`
    - spec file contains `context 'with classification metrics enabled'`
    - spec file contains `context 'with classification metrics disabled (default)'`
    - spec file contains `'enable_orchestrator_metrics' => true`
    - spec file contains `'enable_classification_metrics' => true`
    - spec file contains `.with_content(%r{collect_orchestrator_jobs})`
    - spec file contains `.with_content(%r{collect_node_groups})`
    - spec file contains `compile.and_raise_error` for orchestrator_jobs_limit boundary tests
    - All six contexts appear BEFORE `context 'resource ordering'`
  </acceptance_criteria>
  <done>Six test contexts from the feature branch are present in the spec file at the correct nesting depth</done>
</task>

<task type="auto">
  <name>Task 2: Run rspec-puppet test suite and fix any failures</name>
  <files>
    spec/classes/puppet_data_connector_enhancer_spec.rb
    manifests/init.pp
    templates/puppet_data_connector_enhancer.epp
  </files>
  <read_first>
    spec/classes/puppet_data_connector_enhancer_spec.rb
    manifests/init.pp
    templates/puppet_data_connector_enhancer.epp
  </read_first>
  <action>
Run the full rspec-puppet test suite to validate the merge:

```bash
bundle exec rake spec
```

If `bundle` is not available or Gemfile.lock is missing, try:
```bash
bundle install && bundle exec rake spec
```

If PDK is available as a Docker alias, alternatively:
```bash
pdk test unit
```

Expected outcome: ALL tests pass, including:
- Existing base parameter tests (pre-existing)
- Custom queries tests (from self_service_graphs branch)
- Orchestrator metrics tests (newly added from feature branch)
- Classification metrics tests (newly added from feature branch)
- Boundary validation tests (orchestrator_jobs_limit 0 and 201 rejected)
- Resource ordering tests (pre-existing)

If any tests FAIL, diagnose based on the error:

1. **EPP parameter mismatch** (per research pitfall 3): Check that every EPP parameter
   in the template's `<%- | ... | -%>` block has a matching key in the manifest's `epp()` call.
   Count params in both and ensure they match.

2. **Template content not found** (regex not matching): Check the actual template output
   by examining what the EPP generates. Verify collector method names, config key names,
   and feature flag values match the test regex patterns.

3. **Compile error for valid params**: Check parameter types in manifest match Hiera defaults
   and test values. Boolean params must be `true`/`false` (not strings).

4. **RSpec nesting errors**: Check that test context blocks are properly closed with `end`
   and sit at the correct nesting depth.

Fix any issues found and re-run until all tests pass. Do NOT modify the test assertions
themselves (per D-07, keep feature branch tests as-is). Fix the source code if tests
reveal integration issues from the merge.
  </action>
  <verify>
    <automated>bundle exec rake spec 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bundle exec rake spec` exits with code 0
    - Test output contains "0 failures"
    - Test output shows orchestrator-related examples passing (grep for "orchestrator" in test output)
    - Test output shows classification-related examples passing (grep for "classification" in test output)
    - No conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`) in any of the four target files
  </acceptance_criteria>
  <done>Full rspec-puppet test suite passes with zero failures, validating that the orchestrator and classification merge is correct</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced by test code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Test assertions | accept | Tests are rspec-puppet manifest compilation tests, not security-sensitive; per D-07 kept as-is from feature branch |
</threat_model>

<verification>
After both tasks complete:

1. `bundle exec rake spec` exits 0 with 0 failures
2. Orchestrator test contexts present and passing
3. Classification test contexts present and passing
4. Boundary validation tests present and passing (jobs_limit 0 and 201 rejected)
5. Pre-existing tests (base params, custom queries, resource ordering) still pass
</verification>

<success_criteria>
- All rspec-puppet tests pass (0 failures)
- Six new test contexts cover orchestrator and classification metrics
- Boundary validation confirms Integer[1, 200] constraint on orchestrator_jobs_limit
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-orchestrator-and-classification-metrics/01-02-SUMMARY.md`
</output>
